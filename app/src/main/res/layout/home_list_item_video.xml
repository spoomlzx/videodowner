<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cv_video"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/card_view_margin"
    android:layout_marginTop="@dimen/card_view_margin"
    app:cardCornerRadius="6dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <org.nudt.player.component.RatioImageView
            android:id="@+id/iv_video_pic"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scaleType="centerCrop"
            app:riv_height_to_width_ratio="0.625"
            tools:srcCompat="@drawable/default_image"/>

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/card_view_text_margin"
            android:layout_marginBottom="@dimen/card_view_text_margin"
            android:ellipsize="end"
            android:maxLines="2"
            android:paddingStart="@dimen/card_view_text_margin"
            android:paddingEnd="@dimen/card_view_text_margin"
            android:textSize="12sp" />
    </LinearLayout>

</androidx.cardview.widget.CardView>